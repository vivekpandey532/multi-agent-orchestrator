package com.enterprise.orchestrator.model;

/**
 * Enumerates the specialization roles an agent can assume inside the orchestration hierarchy.
 */
public enum AgentRole {
    MANAGER,
    HISTORY,
    DESIGNER,
    RESEARCHER,
    CODER,
    REVIEWER,
    TESTER
}
