package com.enterprise.orchestrator.model;

/**
 * Records for orchestration requests/responses are split into dedicated files because
 * Java only allows one public top-level type per source file.
 */
